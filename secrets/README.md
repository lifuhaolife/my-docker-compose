# Secrets 配置示例

此目录包含所有敏感配置文件的模板,请复制并填入实际密码后使用。

## 🔐 配置文件列表

### 数据库配置
- `database/.env.mysql` - MySQL 密码配置
- `database/.env.postgres` - PostgreSQL 密码配置
- `database/.env.mongodb` - MongoDB 密码配置
- `database/.env.elasticsearch` - Elasticsearch 密码配置

### 缓存配置
- `cache/.env.redis` - Redis 密码配置

### 中间件配置
- `middleware/.env.rabbitmq` - RabbitMQ 密码配置
- `middleware/.env.nacos` - Nacos 密码配置

### 通用配置
- `.env.common` - 通用环境变量

## ⚠️ 重要提示

1. **不要提交实际密码到公开仓库**
2. 将此目录作为 Git Submodule 指向你的私有仓库
3. 使用强密码 (建议 16位以上,包含大小写字母、数字、特殊字符)
4. 定期更新密码
5. 为不同环境使用不同密码

## 📝 配置模板

参见 `templates/` 目录下的示例文件
