# Secrets 配置管理

此目录包含所有敏感配置文件的模板和实际配置。

## 🔐 配置文件列表

### 数据库配置
- `database/.env.mysql` - MySQL 密码配置
- `database/.env.postgres` - PostgreSQL 密码配置

### 缓存配置
- `cache/.env.redis` - Redis 密码配置

### 中间件配置
- `middleware/.env.rabbitmq` - RabbitMQ 密码配置
- `middleware/.env.nacos` - Nacos 配置

### 通用配置
- `.env.common` - 通用环境变量

## 🚀 快速开始

### 方法 1: 自动生成密码 (推荐)

```bash
# 在项目根目录执行
./scripts/generate-passwords.sh
```

脚本会自动:
- 从模板创建所有配置文件
- 生成强随机密码
- 替换所有占位符
- 显示生成的密码信息

### 方法 2: 手动配置

```bash
# 复制模板
cp secrets/templates/database/.env.mysql.example secrets/database/.env.mysql

# 编辑文件,替换密码占位符
vi secrets/database/.env.mysql
```

### 方法 3: 部署时自动生成

部署时 `bootstrap.sh` 会自动检测并生成密码:

```bash
# 远程部署时自动生成
curl -fsSL https://raw.githubusercontent.com/USER/REPO/main/bootstrap.sh | bash
```

## ⚠️ 重要提示

1. **不要提交实际密码到公开仓库** (已在 .gitignore 中配置)
2. 使用强密码 (自动生成的密码已满足要求)
3. 定期更新密码
4. 为不同环境使用不同密码
5. 将 secrets/ 推送到私有仓库备份 (可选)

## 📝 配置模板

参见 `templates/` 目录下的 `.example` 文件
